<%- include("templates/header") %>
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/assets/common.css" />
    <link rel="stylesheet" href="/assets/login.css" />
    </head>

    <body>
        <%- include("templates/nav") %>
            <!-- <h1>RESET YOUR PASSWORD</h1> -->

            <!-- <form action="/resetPassword" method="post">
        <input type="hidden" name="token" value="<%= token %>">
        <input type="hidden" name="email" value="<%= email %>">
        <input type="password" name="password" placeholder="Enter a new password">
        <input type="password" name="confirmPassword" placeholder="Confirm your new password">
        <button type="submit" name="reset-password-submit">Reset password</button>
    </form>    -->
    <% if (typeof error !=='undefined' && error==='PasswordNotMatch' ) { %>
        <div class="alert alert-danger d-flex justify-content-center mx-auto mb-0" id="alertBox"
            style="width: 20%;" role="alert">
            <p class="alert-heading">Password is not match.</p>
        </div>
        <% } %>

            <main class="form-signin w-100 m-auto">
                <h3 class="resetPassword text-center font-weight-bold pt-2">Reset Password</h3>
                <div class="d-flex gap-2 justify-content-center pb-5 pt-3">
                    <form action="/resetPassword" method="post">
                        <input type="hidden" name="token" value="<%= token %>">
                        <input type="hidden" name="email" value="<%= email %>">
                        <div class="form-group">
                            <label for="formGroupExampleInput" class="text-xs">Password</label>
                            <div class="input-group">
                                <input type="password" class="form-control" name="password" placeholder="">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="formGroupExampleInput2" class="text-xs">Confirm password</label>
                            <div class="input-group">
                                <input type="password" name="confirmPassword" class="form-control"
                                    id="formGroupExampleInput2" placeholder="">
                            </div>
                        </div>
                        <div class="text-center">
                            <button class="btn btn-block resetConfirmBtn" type="submit"
                                name="reset-password-submit">Reset Password</button>
                        </div>
                    </form>
                </div>
            </main>








                    <script src="/scripts/resetPassword.js"></script>
                    <%- include("templates/bottomNav") %>
    </body>